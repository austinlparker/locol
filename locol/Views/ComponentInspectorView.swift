import SwiftUI
import GRDB
import GRDBQuery

// MARK: - DEPRECATED - Replaced with simpler configuration views
// This file is temporarily disabled during the model migration

/*
struct ComponentInspectorView: View {
    // Deprecated - will be replaced with a simpler configuration view
    var body: some View {
        Text("Component Inspector - Under Development")
            .foregroundColor(.secondary)
    }
}

#Preview {
    ComponentInspectorView()
        .frame(width: 350, height: 600)
}
*/